import React from 'react';
import { Nav } from 'reactstrap';
import classes from './header.module.css';

const Header = () => {
  return (
    <Nav className={classes.header}>KMeans Machine Learning Illustration</Nav>
  );
};

export default Header;
